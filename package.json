{
  "name": "coa-bc-steep-slope-calculator",
  "version": "1.0.0",
  "description": "A calculator for determining the slope of a property in the City of Asheville, NC",
  "main": "lambda.js",
  "type": "module",
  "dependencies": {
    "@vendia/serverless-express": "^4.10.4",
    "cookie-parser": "^1.4.5",
    "cors": "^2.8.5",
    "debug": "^4.3.1",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "hbs": "^4.1.1",
    "https": "^1.0.0",
    "morgan": "^1.10.0",
    "pg": "^8.11.3"
  },
  "scripts": {
    "start": "node src/app.local.js",
    "clean": "rm -rf deploy/nodejs && rm -rf deploy/function && rm -rf deploy/.terraform && rm -rf deploy/.terraform.lock.hcl && rm -rf deploy/*.zip",
    "build-layer": "cd deploy && mkdir -p nodejs && cp -R ../package*.json nodejs/ && cd nodejs && npm install --omit=dev",
    "build-function": "cd deploy && mkdir -p function && cp -R ../src/* ../package*.json function/",
    "deploy-terraform": "cd deploy && terraform init && terraform apply -auto-approve -var-file=ca.tfvars",
    "deploy": "cd deploy && npm run build-layer && npm run build-function && npm run deploy-terraform",
    "destroy": "cd deploy && mkdir -p nodejs && mkdir -p function && terraform init && terraform destroy -auto-approve -var-file=ca.tfvars"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cityofasheville/coa-bc-steep-slope-calculator.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/cityofasheville/coa-bc-steep-slope-calculator/issues"
  },
  "homepage": "https://github.com/cityofasheville/coa-bc-steep-slope-calculator#readme"
}